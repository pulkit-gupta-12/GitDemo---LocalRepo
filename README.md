# I have created this file on vscode and then pushed it to github
1. git branch ---- to see all your branches
2. git checkout -b <new branch name> -------- to create new branch, it will become your current branch
3. git checkout <destination branch name> --- to switch to other branch
4. git branch -d <branch name>  --- to delete a branch
5. 